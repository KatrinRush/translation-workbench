# Ручні одноразові експериментальні скрипти в scripts/ - не pytest-тести
# (деякі роблять sys.exit() при імпорті, якщо нема потрібних env-змінних,
# що валить весь збір тестів з INTERNALERROR). Виключаємо з автозбору.
collect_ignore = ["scripts"]
