1  # Ручні одноразові експериментальні скрипти в scripts/ — не pytest-тести
2  # (деякі роблять sys.exit() при імпорті, якщо нема потрібних env-змінних,
3  # що валить весь збір тестів з INTERNALERROR). Виключаємо з автозбору.
4  collect_ignore = ["scripts"]
